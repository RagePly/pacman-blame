[package]
name = "pacman-blame"
version = "0.2.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
alpm = { version = "=4.0.4", features = ["checkver", "generate"] }
